[package]
name = "derive-paste-png"
version.workspace = true
edition.workspace = true
publish.workspace = true

# 解码只发生在 worker 子进程：本工具只做编排与 IO，绝不依赖 image。
[dependencies]
store = { path = "../../crates/store" }
worker = { path = "../../crates/worker" }

[dev-dependencies]
tempfile = "3"
