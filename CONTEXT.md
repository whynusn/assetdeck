# 素材管理器

类 Eagle 的 Windows 素材管理器 + IM 粘贴发送器（双线并行 MVP）。本文件是领域词汇表：统一用语，不含实现细节——实现决策见 DECISIONS.md。

## Language

### 核心概念

**素材（Asset）**:
复制入库后的媒体正本及其元数据，涵盖图片/视频/文本。缩略图、pHash 等派生数据均从正本产出。
_Avoid_: 文件、图片

**素材库（Library）**:
用户素材的正本仓库（.library 包），数据自洽、可整体迁移。
_Avoid_: 库文件夹

**上框**:
把素材送进目标 IM 输入框的核心动作。终点止于「素材进入输入框」，绝不合成回车。
_Avoid_: 发送、直发

**待分类**:
素材未指定归类时落入的缺省收件箱分类。

### 删除

**回收站**:
素材删除后的库内软删除区，不占浏览与搜索结果，可恢复。
_Avoid_: 垃圾桶、trash

**彻底删除**:
从回收站起不可恢复地清除素材正本与全部派生数据。
_Avoid_: 真删除、永久删除

### 选择

**多选模式**:
显式开启的连续选中状态：期间单击 = 选中/取消，上框动作被屏蔽；退出即恢复常态。
_Avoid_: 批量模式、编辑模式

### 搜索

**搜索范围**:
一次搜索应用的字段集合（全部/文件名/分类/标签）。
_Avoid_: 搜索类型

### 导入归类

**归类**:
决定一批新导入素材归属哪个分类的动作，发生在批次级（每批一次，非逐张）。
_Avoid_: 分类操作

**包内分类**:
.emo 包或千牛结构目录内部自带的分类信息。可解析（探得 ≥1 个分类）的包导入时静默按它直通入库，不弹窗。
_Avoid_: 原分类、内置分类

**归入**:
把整批素材放进一个已有分类；弹窗内按名检索候选，不逐个滚动找。
_Avoid_: 统一归入、手动分类（歧义：易被理解为逐张选择）

**新建**:
导入弹窗里输入新分类名并归入。文件夹组默认预填目录名——只是预填建议，不再自动按名归类。
_Avoid_: 按文件夹名归类、目录规则、路径归类

### 外观

**主题 token**:
UI 样式的最小命名变量（颜色/字号/间距），界面只消费 token，不写死值。
_Avoid_: 样式变量、CSS 变量

**主题包**:
由一组主题 token 取值构成的外置主题定义文件，放入主题目录即生效。
_Avoid_: 皮肤、skin
