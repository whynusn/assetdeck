[package]
name = "index"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
domain = { path = "../domain" }
roaring = "0.10"

[dev-dependencies]
proptest = "1"
criterion = "0.5"
# name_search_spec 用 load_real_library 造真库 fixture（集成测试侧允许反向测试依赖）。
ui-viewmodels = { path = "../ui-viewmodels" }
store = { path = "../store" }
domain = { path = "../domain" }

[[bench]]
name = "budget"
harness = false
