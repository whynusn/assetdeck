[package]
name = "index"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
domain = { path = "../domain" }
roaring = "0.10"

[dev-dependencies]
proptest = "1"
criterion = "0.5"

[[bench]]
name = "budget"
harness = false
