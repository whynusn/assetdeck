[package]
name = "ui-viewmodels"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
domain = { path = "../domain" }
index = { path = "../index" }
lru = "0.12"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "layout"
harness = false
