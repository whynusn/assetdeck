# Logging Guidelines — targets

- 本 crate 是纯逻辑层，默认不直接初始化或写日志；调用方根据状态迁移决定观测级别。
- 禁止记录完整窗口标题、会话名、账号 hint、剪贴板内容或哨兵读回正文。
- 可记录的诊断字段仅限：profile id、是否 fallback、候选数量、匹配结果类别、是否发生重绑。
- HWND 仅用于短期诊断，不得作为持久化身份或跨会话关联键。
- 歧义、拒绝自动重绑、L2/L3 失败应由上层记录结构化原因，用户界面继续使用人话反馈。
